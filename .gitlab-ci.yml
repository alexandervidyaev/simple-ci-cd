stages:
    - build

Build container:
  stage: build
  tags:
    - docker-runner
  image: docker:20.10.18
  services:
    - docker:dind
  script:
    - docker build -t ${CI_REGISTRY}/${GITLAB_USER_ID}/${CI_PROJECT_NAME} .
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN $CI_REGISTRY
  
