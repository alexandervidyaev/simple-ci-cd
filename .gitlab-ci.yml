stages:
  - build
  - deploy

variables:
  NAME: my-nginx

Build container:
  stage: build
  tags:
    - docker-runner
  image: docker:20.10.18
  services:
    - docker:dind
  script:
    - docker build -t $CI_REGISTRY/$GITLAB_USER_LOGIN/$CI_PROJECT_NAME/$NAME:$CI_COMMIT_SHORT_SHA .
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN $CI_REGISTRY
    - docker push $CI_REGISTRY/$GITLAB_USER_LOGIN/$CI_PROJECT_NAME/$NAME:$CI_COMMIT_SHORT_SHA

Deploy container:
  stage: deploy
  tags:
    - docker-runner
  image: alpine:3.15
  script:
    - apk update && apk add openssh-client
    - chmod 400 $SSH_KEYGEN
    - ssh -i $SSH_KEYGEN-o StrictHostKeyChecking=no $USER@$SERVER "echo 1 > /home/deployer/lol.txt"










